[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[phases.setup]
nixPkgs = ["gcc", "libgl1", "libglib2.0"]

[start]
cmd = "PYTHONPATH=backend uvicorn app.main:app --host 0.0.0.0 --port 8000"
